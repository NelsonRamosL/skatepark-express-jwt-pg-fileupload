    <h1>Skate Park</h1>

    <div class="py-4">
      <h2>Registro</h2>
      <hr class="w-50" />

      <form method="POST" action="/agregar" enctype="multipart/form-data">
        <div class="form-group row w-50 m-auto">
          <div class="form-group col-12 col-sm-6">
            <label>Email</label>
            <input class="form-control m-auto" name="email" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Nombre</label>
            <input class="form-control m-auto" name="nombre"/>
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Password</label>
            <input class="form-control m-auto" name="password" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Repita la password</label>
            <input class="form-control m-auto" name="password2" />
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Años de experiencia</label>
            <input class="form-control m-auto" name="anos"/>
          </div>
          <div class="form-group col-12 col-sm-6">
            <label>Especialidad</label>
            <input class="form-control m-auto" name="especialidad" />
          </div>
          <div class="form-group col-12 col-sm-12">
            <label>Foto de perfil</label>
            <input type="file" id="foto" name="foto"/>
          </div>
        </div>
      <input type="submit" value="Registarme">
        {{!-- <button class="btn btn-info mb-3">Registrarme</button> --}}
        <p><a href="./login"> Iniciar sesión</a></p>
      </form>
    </div>



<script>
/** $('button').click(async (e) =>{
  e.preventDefault;
const email = $('#email').val();
const nombre = $('#nombre').val();
const password = $('#password').val();
const password2 = $('#password2').val();
const anos = $('#anos').val();
const especialidad = $('#especialidad').val();
const fotoarchivo = $('#fotoarchivo').val();

if (password != password2) {
  alert("tiene que ingresar bien la passwd");
exit;
    }

const payload = { email,nombre,password,anos,especialidad,fotoarchivo }
try {
await axios.post('/agregar', payload);
alert('Skaiter registrado con exito');
window.location.href = "/login";
} catch ({ response }){
const { data } = response;
const { error } = data;
alert(error);
}

}) 

*/

</script>